Integration tests that involve media uploads use real R2 credentials from `op://tx-agent-kit-services/dev/R2_*` — these are configured as GitHub Actions secrets (R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_ENDPOINT) and injected via `.github/workflows/integration-tests.yml`.
