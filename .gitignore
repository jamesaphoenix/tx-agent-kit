node_modules
.playwright-cli
.pnpm-store
.turbo
dist
coverage
.vitest
.env
.env.*
!.env.example
!.env.playwright.dev
.next
.DS_Store
*.log
*.map
*.tsbuildinfo
deploy/.rendered
deploy/artifacts
apps/*/.next
apps/*/dist
packages/*/dist
packages/infra/db/drizzle.config.js
packages/infra/db/drizzle.config.js.map
packages/infra/db/drizzle.config.d.ts
packages/infra/db/drizzle.config.d.ts.map
pgdata
.data/
.vercel

# Vendor / competitor reference apps (swappable via tx pin)
.vendor/

# Secondary git worktrees live under .worktrees/ at the primary checkout.
# Ignoring the directory keeps `git status` on the primary from showing
# worktree internals as untracked candidates.
.worktrees/
worktrees

# Worktree-scoped helper scripts generated by scripts/worktree/setup.sh.
# Each worktree's copy contains a hardcoded absolute path — they should
# never be committed.
/run-migrations.sh
/reset-worktree-schema.sh
.diffcore/
