/**
 * Colocation stub for `temporal-lib-resolution.integration.test.ts`.
 *
 * That integration test exercises the resolution of `@temporalio/*` libraries
 * across the monorepo (ensuring no version drift) and does not import from a
 * dedicated source module. This file exists only to satisfy the test-
 * colocation invariant (`enforce-domain-invariants` expects a source file
 * peer to every `*.test.ts`).
 *
 * Do not import this file.
 */
export {}
