/**
 * Colocation stub for `billing.properties.test.ts`.
 *
 * The property-based tests in `billing.properties.test.ts` exercise the
 * pure margin / cost-aggregation math exported from `billing.ts` directly,
 * so there is no standalone source module needed here. This file exists
 * only to satisfy the test-colocation invariant (`enforce-domain-invariants`
 * expects a source file peer to every `*.test.ts`).
 *
 * Do not import this file — import from `./billing.js` instead.
 */
export {}
