# Local MCP endpoints for observability.
# Copy to ".env.mcp" and tweak if your ports/hosts differ.

PROMETHEUS_URL=http://localhost:9090
# Optional: pin/override image tag.
# PROMETHEUS_MCP_IMAGE=ghcr.io/pab1it0/prometheus-mcp-server:latest

# jaeger-mcp-server supports HTTP or GRPC. For local compose UI/API, use HTTP.
JAEGER_URL=http://localhost:16686
JAEGER_PROTOCOL=HTTP
JAEGER_PORT=16686
